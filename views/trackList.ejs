<<<<<<< HEAD
git 
=======
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MusicMETA</title>
    <link rel="icon" type="image/jpg" href="img/CD-image.jpg">
    <link rel="stylesheet" href="/styles/style.css">
</head>


<body> 
 
  <h1><%= albumName %></h1> 
  <p>Total tracks: <%= trackCount %></p> 
  
  
  <h2>Track List</h2> 
  <table> 
    <thead> 
      <tr> 
        <th>Track #</th> 
        <th>Track Name</th> 
        <th>Duration</th> 
      </tr>
    </thead>
    <tbody> 
      <% tracks.forEach(track => { %> 
        <tr> 
          
          <td><%= track.trackNum %></td> 
          <td><%= track.TrackName %></td>
          <td><%= track.track_length %></td>
        </tr>
      <% }); %> 
    </tbody>
  </table>


<body>
    <div id = headerTitle>
        <form action="/" method="post">
            <button type="submit" id = navi>🔙 Go Home</button>
        </form>
        <H2>Album Track List</H2>
        <button onclick="history.go(-1)" id = navi>🔙 Go Back</button>
    </div>
    <!-- <p>Check console log for TrackList</p> -->
    <div id="trackInfo">
        <img src="<%= imgPath %>" id="trackImg" alt="album cover">
          <% if (tracks && tracks.length > 0) { %>
            <!-- Track listing section -->
            <div class="track-list">
                <!-- Display total tracks from album_single table -->
                <p>Total Tracks: <%= album.TrackCount %></p>
                <table class="track-table">
                    <thead>
                        <tr>
                            <!-- Table headers matching track_info columns -->
                            <th>Track #</th>  
                            <th>Track Name</th> 
                            <th>Duration</th>   
                        </tr>
                    </thead>
                    <tbody>
                        <% tracks.forEach(track => { %>
                            <!-- Dynamic table rows for each track -->
                            <tr>
                                <td><%= track.TrackNum %></td>
                                <td><%= track.TrackName %></td>
                                <!-- Display formatted time from TIME datatype -->
                                <td><%= track.track_length %></td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        <% } else { %>
            <!-- Fallback content if no tracks found -->
            <p class="no-tracks">No tracks found for this album.</p>
        <% } %>
    </div>


        <!-- Add thing here -->
        <% tracks.forEach(track => { %> 
            <tr> 
              
              <td><%= track.trackNum %></td> 
              <td><%= track.TrackName %></td>
              <td><%= track.track_length %></td>
            </tr>
          <% }); %> 
    </div>

</body>
</html>

