<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MusicMETA</title>

    <link rel="stylesheet" href="/styles/style.css">
</head>
<body id = titleMain>
    <div>
        <header>
            <h1>MusicMETA</h1>
            <p>Simplist page for Music Info</p>
        </header>
    </div>

    <div id = searchEngine>
        <form action="search" method="post">
            <input type="text" name="userInput" placeholder="Search Artist..." required id="userInput">
            <button type="submit" id = navi>🔍</button>
            
        </form>
        <% if (typeof error !== 'undefined' && error) { %>
            <span class="err" id="err-name"><%= error %></span>
        <% } %>
    </div>
</body>
</html>